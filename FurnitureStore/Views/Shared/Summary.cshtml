@model FurnitureStore.Entities.Furniture
    
    
           
    <div class="image">
        @foreach (var i in Model.Images)
        {
            if (i.IsMainImage == true)
            {

                <a href="@Url.Action("Details" , "Furniture" , new { id = Model.FurnitureId })">

                    <img src="@Url.Content(i.Path)" alt="@i.DisplayName" width="235" height="235" />
                </a>

            }
        }
    </div>


                <div class="name_product">

                    @Html.ActionLink(@Model.Name, "Details", new { id = Model.FurnitureId } , new { @style = "color: #776655; text-decoration:none;" })
                </div>
                <div class="about">
                   


                    <div class="about_right_column">
                        <div class="price"><h4>@Model.Price.ToString("# грн")</h4></div>
                    </div>
                </div>
                @using (Html.BeginForm("AddToCart", "Cart", new { Id = Model.FurnitureId }))
                {
                    @Html.HiddenFor(modelitem => Model.FurnitureId)
                    @Html.Hidden("returnUrl", Request.Url.PathAndQuery)
                   <input type = "submit" class="buy btn" value="В кошик" />
               


                }
